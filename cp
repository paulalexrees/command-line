The cp shell command copies a file or directory.
If no location is specified, a copy is made in the same directory
eg.
cp aFile copiedFile

The recursive -r switch must be used when copying directories in order to copy all files within it.
